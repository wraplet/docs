export const WRAPLET_VERSION = "1.0.0-beta.23";
export const WRAPLET_URL = `https://cdn.jsdelivr.net/npm/wraplet@${WRAPLET_VERSION}+esm`;
export const WRAPLET_DIST_URL = `https://cdn.jsdelivr.net/npm/wraplet@${WRAPLET_VERSION}/dist`;
export const WRAPLET_DIST_ESM_URL = `${WRAPLET_DIST_URL}+esm`;